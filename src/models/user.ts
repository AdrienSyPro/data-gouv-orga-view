export default interface User {
    avatar: string | null,
    class: "User",
    email: string | null,
    first_name: string,
    last_name: string,
    page: string,
    slug: string,
    uri: string
}